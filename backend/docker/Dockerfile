# Используйте официальный образ Node.js 20.11.1
FROM node:20.11.1

# Создайте директорию для приложения
WORKDIR /usr/src/app

# Откройте порт, который будет использоваться приложением
EXPOSE 3001

# Команда-заглушка для оставания контейнера в рабочем состоянии
CMD ["tail", "-f", "/dev/null"]